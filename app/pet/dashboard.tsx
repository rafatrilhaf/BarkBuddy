// app/pet/dashboard.tsx
import { Text, View } from "react-native";
export default function PetDashboard() {
  return (
    <View style={{ flex:1, alignItems:"center", justifyContent:"center" }}>
      <Text>Dashboard do Pet (em breve)</Text>
    </View>
  );
}
